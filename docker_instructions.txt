# bulding a new image
docker build -t car_renting_backend .

# running a container
docker run -it -p4000:4000 car_renting_backend

# to go through the directory
docker run -it -p4000:4000 car_renting_backend bash
